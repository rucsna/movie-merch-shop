dotnet user-secrets init

dotnet user-secrets set "JwtIssuerSigningKey" "PASSWORDHERE"

string issuerSigningKey = Configuration["JwtIssuerSigningKey"];
var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(issuerSigningKey));